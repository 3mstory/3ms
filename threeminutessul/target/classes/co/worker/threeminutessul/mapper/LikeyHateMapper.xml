<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="likeyhate">
	<insert id="insertLikey" parameterType="likeyVo">
		insert into tblLikey (userSeq, boardSeq, isdupl) values(#{userSeq},#{boardSeq},#{isdupl});
	</insert>
	
	<insert id="insertHate" parameterType="hateVo">
		insert into tblHate (userSeq, boardSeq, isdupl) values(#{userSeq},#{boardSeq},#{isdupl});
	</insert>
</mapper>